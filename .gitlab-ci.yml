image: golang:1.8

test:
  script:
  - go get -u github.com/golang/dep/...
  - mkdir -p $GOPATH/src/github.com/davars
  - cp -a /builds/davars/sohop $GOPATH/src/github.com/davars/sohop
  - cd $GOPATH/src/github.com/davars/sohop
  - dep ensure
  - go test $(go list ./... | grep -v /vendor/)